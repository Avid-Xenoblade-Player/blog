/*
const h1Element = document.querySelector('h1');
const textLength = h1Element.textContent.length;

// Adjust the base typing speed as needed
const baseTypingSpeed = 1000; // milliseconds per character

const typingDuration = textLength * baseTypingSpeed;
h1Element.style.setProperty('--typing-duration', `${typingDuration}ms`);
*/